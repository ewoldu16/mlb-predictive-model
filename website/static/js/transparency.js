document.querySelectorAll('[data-feature-filters] button').forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter;document.querySelectorAll('[data-feature-status]').forEach(row=>row.hidden=filter!=='ALL'&&row.dataset.featureStatus!==filter);document.querySelectorAll('[data-feature-filters] button').forEach(item=>item.classList.toggle('active',item===button));}));
